services:
  pricing:
    build: .
    ports:
      - "8001:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-sqlite:///./pricing.db}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
